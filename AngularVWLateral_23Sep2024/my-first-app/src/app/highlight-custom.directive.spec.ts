import { HighlightCustomDirective } from './highlight-custom.directive';

describe('HighlightCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCustomDirective();
    expect(directive).toBeTruthy();
  });
});
